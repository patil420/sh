#include <stdio.h>
#include <stdlib.h>

// Structure for a BST node
struct node {
    int data;
    struct node *left, *right;
};

// Function to create a new node
struct node* createNode(int value) {
    struct node* newNode = (struct node*)malloc(sizeof(struct node));
    newNode->data = value;
    newNode->left = newNode->right = NULL;
    return newNode;
}

// Function to insert a value in BST
struct node* insert(struct node* root, int value) {
    if (root == NULL)
        return createNode(value);
    if (value < root->data)
        root->left = insert(root->left, value);
    else if (value > root->data)
        root->right = insert(root->right, value);
    return root;
}

// Function to find the number of nodes in the longest path (height)
int findHeight(struct node* root) {
    if (root == NULL)
        return 0;
    int left = findHeight(root->left);
    int right = findHeight(root->right);
    return (left > right ? left : right) + 1;
}

// Function to find minimum value in BST
int findMin(struct node* root) {
    if (root == NULL) {
        printf("Tree is empty.\n");
        return -1;
    }
    while (root->left != NULL)
        root = root->left;
    return root->data;
}

// Function to mirror the tree (swap left & right pointers)
void mirrorTree(struct node* root) {
    if (root == NULL)
        return;
    struct node* temp = root->left;
    root->left = root->right;
    root->right = temp;
    mirrorTree(root->left);
    mirrorTree(root->right);
}

// Function to search a value in BST
int search(struct node* root, int key) {
    if (root == NULL)
        return 0;
    if (root->data == key)
        return 1;
    else if (key < root->data)
        return search(root->left, key);
    else
        return search(root->right, key);
}

// Function to display tree in inorder (sorted order)
void inorder(struct node* root) {
    if (root != NULL) {
        inorder(root->left);
        printf("%d ", root->data);
        inorder(root->right);
    }
}

int main() {
    struct node* root = NULL;
    int choice, value, n, i;

    printf("=== 🌳 Binary Search Tree Operations ===\n");

    // Step 1: Construct BST from given values
    printf("Enter number of values to insert initially: ");
    scanf("%d", &n);
    printf("Enter %d values:\n", n);
    for (i = 0; i < n; i++) {
        scanf("%d", &value);
        root = insert(root, value);
    }

    do {
        printf("\nMenu:\n");
        printf("1. Insert New Node\n");
        printf("2. Find Number of Nodes in Longest Path (Height)\n");
        printf("3. Find Minimum Data Value\n");
        printf("4. Mirror the Tree (Swap Left & Right)\n");
        printf("5. Search a Value\n");
        printf("6. Display Inorder Traversal\n");
        printf("7. Exit\n");
        printf("Enter your choice: ");
        scanf("%d", &choice);

        switch (choice) {
        case 1:
            printf("Enter value to insert: ");
            scanf("%d", &value);
            root = insert(root, value);
            printf("✅ %d inserted successfully.\n", value);
            break;

        case 2:
            printf("🌲 Longest path (height) = %d nodes\n", findHeight(root));
            break;

        case 3:
            printf("🔹 Minimum value in tree: %d\n", findMin(root));
            break;

        case 4:
            mirrorTree(root);
            printf("🔄 Tree has been mirrored.\n");
            break;

        case 5:
            printf("Enter value to search: ");
            scanf("%d", &value);
            if (search(root, value))
                printf("✅ Value %d found in the tree.\n", value);
            else
                printf("❌ Value %d not found.\n", value);
            break;

        case 6:
            printf("📋 Inorder Traversal (sorted order): ");
            inorder(root);
            printf("\n");
            break;

        case 7:
            printf("Exiting program. Goodbye!\n");
            break;

        default:
            printf("Invalid choice! Try again.\n");
        }
    } while (choice != 7);

    return 0;
}
